{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Gov Salaries Exchange Rates",
  "type": "object",
  "required": ["baseCurrency", "timestamp", "rates"],
  "properties": {
    "baseCurrency": {
      "type": "string",
      "description": "The base currency for conversion (e.g. USD)"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO timestamp when the rates were recorded"
    },
    "rates": {
      "type": "object",
      "description": "Currency conversion rates relative to baseCurrency",
      "additionalProperties": {
        "type": "number",
        "description": "Exchange rate value (1 baseCurrency = X targetCurrency)"
      }
    }
  }
}
